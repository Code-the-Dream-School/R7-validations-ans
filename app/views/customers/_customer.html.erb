<div id="<%= dom_id customer %>">
  <p>
    <strong>Full name:</strong>
    <%= customer.full_name %>
  </p>

  <p>
    <strong>Phone:</strong>
    <%= customer.phone %>
  </p>

  <p>
    <strong>Email:</strong>
    <%= customer.email %>
  </p>

  <% if customer.orders.empty? %>
    <p><strong>There are no orders for this customer.</strong></p>
  <% else %>
    <h2>Orders</h2>
    <table>
      <thead>
        <tr>
          <th>Product name</th>
          <th>Product count</th>
          <th colspan="3"></th>
        </tr>
      </thead>
      <tbody>
        <% customer.orders.each do |order| %>
          <tr>
            <td><%= order.product_name %></td>
            <td><%= order.product_count %></td>
            <td><%= link_to 'Show', order %></td>
            <td><%= link_to 'Edit', edit_order_path(order) %></td>
            <td><%= button_to 'Destroy', order, method: :delete, form: { data: { turbo_confirm: "Are you sure?" } } %></td>
          </tr>
        <% end %>
      </tbody>
    </table>
  <% end %>

</div>
